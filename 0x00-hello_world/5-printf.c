#include <stdio.h>
/**
 * main- entry point for this program
 *
 * Return: prints the value zero (0) if no error or otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
