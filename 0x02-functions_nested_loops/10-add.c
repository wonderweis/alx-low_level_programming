#include "main.h"
#include <stdio.h>

/**
 * add- function that adds two integers and returns the result
 * @i: print int i
 * @k: print int k
 *
 * Return: value of 0
 */

int add(int i, int k)
{
	return (i + k);
}
